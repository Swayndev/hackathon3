<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class OwnersController extends Controller
{
    //

    public function index()
    {

        // $owners = Owner::limit(20) // LIMIT 20
        // ->where('first_name', '>', 5000) // WHERE `votes_nr` > 5000
        // ->where('surname', )  // AND
        // ->get();

        // return view('movie.index', compact('owners'));
        // //                       ['movies' => $movies]
    }
}
